# 大喜利カードゲーム 正式仕様書 (MASTER)
※本ファイルはゲーム実装・改修の基準となる公式仕様書（Single Source of Truth）です。
※AIおよび開発者は、本仕様書に記述されたルールおよび設計を最優先として実装・機能追加を行ってください。

---

## 1. ゲーム内テキスト（文言）の統一と管理規約

### ■ 適用範囲
本規約は、ゲーム内でプレイヤーに対して表示されるすべてのテキスト表現および文言に適用される。
1. 各種カード（お題・回答）の内容
2. 各種モーダル、ダイアログ上の見出し、説明文、ボタンテキスト
3. その他、ゲームプレイ中に画面上に表示されるすべてのシステムメッセージ・ステータス表記

### ■ 文言の策定・管理主体
* ゲーム内に表示されるすべての文言は、**仕様策定者（担当者）が直接記述・管理**を行う。

### ■ 変更・改変に関する禁止事項
1. **無断変更・アレンジの禁止**:
   明確な指示または事前の協議がない限り、プログラム実装時およびUIデザイン作成時において、テキスト文言・ニュアンス・言い回しを開発者側で勝手に変更・要約・アレンジすることを禁止する。
2. **システム制約時の事前確認義務**:
   レイアウト崩れや文字数オーバー等のシステム上の制約により、どうしても文言の変更・短縮が必要となる場合は、開発者が独自に判断して変更せず、**必ず事前に仕様策定者へ確認・提案を行い、了承を得た上で修正**を行うこと。

---

## 2. ゲーム基本概要・勝利条件

* **ゲーム形式**: あらかじめ用意された回答カードから手札を選び、AI自動採点によって「一本（勝ち星: ★）」を競うリアルタイムカードバトル。
* **プレイ人数**: 2名〜10名
* **勝利条件**: 「一本（勝ち星）」を**先に3回獲得したプレイヤーの完全勝利**（★★★）。
* **プラットフォーム**: Webブラウザ（PC / スマートフォン対応・レスポンシブWebデザイン）。
* **通信基盤**: Node.js + Socket.io によるリアルタイム双方向通信。

---

## 3. カードデータ管理およびディレクトリ構造

### ■ カードデータの完全分離（`data/cards.js`）
サーバーの通信・採点ロジック（`server.js`）の誤破損を防止し、メンテナンス性を最大化するため、お題・回答カードの全テキストデータは専用モジュール `data/cards.js` に集約・管理する。

### ■ ディレクトリ構造
```text
oogiri-card-game/
├── data/
│   └── cards.js        <-- ★お題・回答カードの追加・編集専用ファイル
├── public/
│   ├── index.html      <-- HTML構造・モーダル・盤面マークアップ
│   ├── style.css       <-- レスポンシブUI・アニメーション・Gridレイアウト
│   └── client.js       <-- Socket.io通信・UI状態遷移・済ランプ制御
├── .env                <-- APIキー・優先モデル設定（Git除外推奨）
├── package.json        <-- 依存関係定義
├── server.js           <-- ゲーム進行・AI審査サーバー（カードデータは外部参照）
└── GAME_SPEC.md        <-- 本公式マスター仕様書
```

### ■ カード追加・編集時のルール
1. **編集対象ファイル**: カードテキストの追加・修正・削除時は、**`data/cards.js` のみ**を編集すること。
2. **書式ルール**:
   * `MASTER_TOPICS`（お題）および `MASTER_ANSWERS`（回答）の配列内に、ダブルクォーテーションで囲んだ文字列を記述する。
   * 末尾のカンマ（`,`）は記述したままで問題ない（JavaScript構文として有効）。
   * 一時的に出現させたくないカードは `// "カード名",` のように行頭にスラッシュ2つを記述してコメントアウト（無効化）が可能。

---

## 4. ロビーおよびプレイヤー管理

1. **エントリー処理（トグル仕様）**:
   * URLアクセス後、「参加する」ボタン押下でエントリー。
   * エントリー中はボタンが「参加キャンセル」に変化し、再押下でキャンセル可能なトグル方式。
2. **リアルタイム同期表示**:
   * 「現在の参加人数（例: 3 / 10名）」を常時リアルタイム同期。
   * エントリー状態に応じてバッジを動的表示（未エントリー: `未エントリー` / エントリー中: `参加中... 他のプレイヤーを待っています`）。
3. **プレイヤー名変更機能**:
   * エントリーの前後を問わず、画面右上の「⚙️ 名前変更」ボタンからモーダルを開き変更可能（12文字以内）。
4. **ホスト権限とゲーム開始**:
   * 最初に接続したプレイヤーが自動的にホスト（👑）となる。
   * ホスト切断時は残りの接続者へ自動的にホスト権限を委譲。
   * エントリー人数が**2名以上**になった場合のみ、ホストの画面に「ゲーム開始（ホストのみ）」ボタンが表示され、押下により即座に第1ラウンドを開始。

---

## 5. デッキ・カード配布ロジック

1. **手札配布と全重複なし（Unique Constraint）**:
   * 初期手札：回答デッキより各プレイヤーへ**7枚**自動配布。
   * **全プレイヤーを通じて同名カードは場に1枚のみ存在**（他プレイヤーが手札に保持しているカードは配られない）。
2. **カードの消費・補充・山札切れリセット**:
   * 提出された回答カードおよび出題されたお題カードは順次破棄。
   * 各ラウンド開始時、手札が7枚未満のプレイヤーに1枚ずつカードを補充（常に手札7枚を維持）。
   * 未使用の山札ストックが無くなった場合のみ、現在他プレイヤーの手札にあるカードを除外した上で再シャッフルして出現状態をリセット。

---

## 6. 回答選択・確定UIフロー

確認ポップアップ（アラート）は廃止し、自陣UI内のボタン確定フローを採用する。

1. **ボタン配置**:
   * 自陣UI内、「プレイヤー情報・勝ち星」と「済ランプ」の中間スペース中央に配置。
2. **ボタンの挙動・状態遷移**:
   * **Ⅰ. 初期（未選択時）**: 「回答する」ボタンは非活性状態（グレーアウト / 押下不可）。
   * **Ⅱ. カード選択時**: 手札からカードを1枚タップすると、カードがハイライト（オレンジ枠・浮上）され、ボタンが活性化（押下可能）。別の手札をタップすると選択が切り替わる。
   * **Ⅲ. 確定時（移動表示）**: 「回答する」ボタンを押下すると回答が確定。カードは即座に消滅せず、**「自陣UI」と「中央お題エリア」の間の中央スペース（あなたの提出した回答カード）へ移動・配置**され、提出状態を維持。自陣の「済」ランプが点灯する。
   * **Ⅳ. 消滅タイミング**: 全員の回答が揃い、採点完了後の**「ラウンドリザルト画面」へ遷移した瞬間に中央のカードが破棄（消滅）**される。

---

## 7. AI自動審査・採点仕様

### ■ 採点基準・基本ルール
* **100点満点・厳格な同点禁止**: 各プレイヤーの回答に対し1〜100点で採点。**全員が完全に異なるスコア（同点不可）**となる。
* **最高得点者に勝ち星付与**: 最も高いスコアを獲得したプレイヤーに勝ち星（★）を1つ付与。
* **一括バッチ採点（Batch Evaluation）**: 全プレイヤーの回答を1つのプロンプトにまとめ、1回のAPIリクエストで全員分を同時採点（待ち時間を2〜3秒に短縮）。
* **AI審査講評の文字数**:
  * 採点理由（なぜウケたか）と愛のあるツッコミ/称賛を交えた構成。
  * **2〜3文、100〜130文字程度**。

### ■ モデル優先順位とフォールバック階層
Google Generative AI（Gemini API）を採用し、応答速度・コスト・耐障害性を考慮した4段階の自動フォールバックを行う。
1. **最優先**: `gemini-3.5-flash-lite`（低遅延・高コスパ）
2. **第2優先**: `gemini-3.1-flash-lite`
3. **第3優先**: `gemini-3.5-flash`
4. **第4優先**: `gemini-3-flash-preview`

### ■ エラーハンドリング・スマートフォールバック
* **429（クォータ上限 / Rate Limit）対策**:
  無料枠（Free Tier）の日次上限（20 requests/day等）や過負荷に達した際は、長時間の再試行待機を行わず、即座に次期モデルへ移行する。
* **スマートフォールバックエンジン**:
  すべてのAPI呼び出しが不能な場合でもゲームを停止させず、**同点なしの採点＋高品質な100〜130文字の講評バンク（20パターン以上）**から自然な審査結果を即座に生成する。

---

## 8. 画面遷移・リザルト・UIレイアウト

### ■ 盤面レイアウト（可変2〜10名対応）
* **画面手前（自陣）**: プレイヤー名、現在の勝ち星（★☆☆）、確定ボタン（回答する）、手札（7枚）、「済」ランプ。
* **画面中央（お題）**: お題カード、審査中ローディング表示（「AIが厳正に審査中...」※モデル名表記なし）。
* **中央下部（提出スロット）**: 自身が確定した回答カードの配置スペース。
* **画面上部〜左右（対戦相手）**: プレイヤー人数（2〜10名）に応じてCSS Flexにより自動均等配置。相手の名前、星、済ランプを表示。

### ■ 「済」ランプの点灯・消灯フロー（未操作者炙り出し用）
1. 回答選択・確定ボタン押下で 【点灯】
2. 全員の回答が揃い、ラウンドリザルト表示で 【消灯】
3. ラウンドリザルトの「次へ」ボタン押下で 【点灯】
4. 次ラウンド開始（お題配布）で 【消灯】
※リザルト画面時も前面に全員の済ランプを表示し、準備状況を常時可視化する。

### ■ ラウンドリザルト画面（3カラムGrid & 10人縦長対策）
* **3カラムGrid構成**:
  * **カラム1（プレイヤー情報）**: 順位 ＋ 現在の勝ち星（★☆☆） ＋ プレイヤー名（※1位は★が増えるポップアニメーション）。
  * **カラム2（得点）**: AIによる採点結果（〇〇点）。
  * **カラム3（回答カード）**: 使用した回答カード（クリックで講評詳細モーダルを表示）。
* **AI解説の表示**:
  * **1位**: 1位と2位の間に「🏆 1位の講評」バナーとして**常時展開表示**。
  * **2位以下**: カラム3の回答カードをクリックすることで、ポップアップモーダル内に講評を表示。
* **10人参加時の縦長対策**:
  * テーブルコンテナに `max-height: 55vh; overflow-y: auto;` を適用し、画面外への突き抜けを防止。
* **次ラウンド遷移**:
  * エントリー者全員が画面下の「次へ（準備完了）」を押下した瞬間に次ラウンドへ自動遷移。

### ■ 最終対戦結果画面（ゲームセット時）
* **遷移条件**: 誰かが3つ目の星（★★★）を獲得したラウンド終了時、全員が「次へ」を押下すると自動遷移。
* **演出・順位一覧**:
  * チャンピオンをスポットライト演出・王冠アイコンで強調表示。
  * 獲得した勝ち星順にランク付け（同勝ち星数のプレイヤーは同順位）。
* **個別決定ガード（不具合防止設計）**:
  * 誰か1人が操作しても、他プレイヤーの画面が勝手にロビーへ引き戻されないよう閲覧状態（`isViewingGameOver`）を保護。
* **アクションボタン**:
  * **「再戦」ボタン**: 押下した本人のみがエントリー状態（参加中...）となり、最終結果画面を閉じてロビーへ遷移。
  * **「退出する」ボタン**: 押下した本人のみが未エントリー状態となり、最終結果画面を閉じてロビーへ遷移。

---

## 9. システム環境・APIキー設定規約

### ■ APIキーの永続化管理（.env方式）
`server.js` の上書き等でAPIキーが消失することを防ぐため、`dotenv` による環境変数管理を必須とする。

* **プロジェクト直下 `.env` ファイル**:
  ```env
  GEMINI_API_KEY=AIzaSy...（実際のAPIキー）
  # 任意指定（未指定時は gemini-3.5-flash-lite がデフォルト）
  GEMINI_MODEL=gemini-3.5-flash-lite
  PORT=3000
  ```
* **依存パッケージ（`package.json`）**:
  * `@google/generative-ai`
  * `dotenv`
  * `express`
  * `socket.io`

---

## 10. 仕様変更・追加開発時の開発ガイドライン
1. **修正前確認**: 機能追加やデザイン変更を行う前に、必ず本 `GAME_SPEC.md` の記述との整合性を確認すること。
2. **Single Source of Truth**: 新たな仕様決定や変更が発生した場合、ソースコードの修正と同時に本仕様書を必ず最新化すること。
3. **コピペ即応性の維持**: ソースコードの出力時は、初心者でもそのまま差し替え可能なようにファイル単位で省略のない全体コードを出力すること。